{
  "name": "contract-tester",
  "version": "1.0.0",
  "description": "for interacting with contracts",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "truffle migrate --reset --network bscTest && truffle migrate --reset --network maticTest && truffle migrate --reset --network rinkeby"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.5.0",
    "@truffle/hdwallet-provider": "^2.0.4",
    "ethers": "^5.6.4",
    "jsdoc": "^3.6.10",
    "truffle-contract-size": "^2.0.1",
    "web3": "^1.7.1"
  }
}
